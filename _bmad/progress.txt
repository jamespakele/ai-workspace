2026-06-08T14:20:34Z | STORY-0001: SM↔PO approved in 1 pass(es)
2026-06-08T14:31:00Z | STORY-0001: scaffolded `hermes-desktop/` with Tauri 2 + React 18 + Vite 5, added Tailwind/shadcn init, stubbed required Rust commands/plugins, and built the static shell. Files modified: `hermes-desktop/*`, `docs/backlog/stories/story-0001-tauri-app-scaffold.md`. Decisions: replaced scaffold defaults to match architecture versions; initialized shadcn and kept generated `src/components/ui/button.jsx`; recorded Linux native dependency blocker instead of forcing completion. QA notes: `npm run build` passes; `cargo build --release` and `cargo tauri dev` remain blocked until Ubuntu packages for `dbus-1`, `webkit2gtk-4.1`, and `librsvg-2.0` are installed.
2026-06-08T14:48:00Z | STORY-0001: verified the remaining blocker is host-native Tauri dependencies, not repo source. Files modified: `docs/backlog/stories/story-0001-tauri-app-scaffold.md`, `_bmad/progress.txt`. Decisions: attempted a user-local package extraction path for `dbus-1`, `webkit2gtk-4.1`, `librsvg-2.0`, `glib`, `gtk`, `libsoup`, `javascriptcoregtk`, `pango`, `gdk-pixbuf`, `cairo`, `atk`, `harfbuzz`, and `fribidi`; stopped when `cargo build --release` still required additional transitive native packages (`libpcre2-8` via `glib-2.0`), which would amount to reprovisioning the host outside story scope. QA notes: `npm run build` still passes; `cargo build --release` advances further with local pkg-config metadata but still fails before app-code compilation completes; story remains `IN_DEV` until the Linux GTK/WebKit toolchain is installed system-wide.

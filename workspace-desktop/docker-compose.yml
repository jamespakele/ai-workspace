services:
  workspace:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - workspace-data:/home/workspace/.workspace
    environment:
      - PORT=3000
    restart: unless-stopped

volumes:
  workspace-data:

[package]
name = "hermes-core"
version = "0.1.0"
edition = "2021"
description = "Pure logic for Hermes Desktop, testable without the Tauri runtime"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
